Run one of the open_ai_chat_GPTXXX.py files depending on which GPT version you wish to use.

"pip install -r reqs.txt"

Also don't forget to insert your api key in api_key.sample.txt, and rename the file to api_key.txt.
For more information, try to run a file without an API key.